let installerHash="",button=document.querySelector("#copy-hash"),status=document.querySelector("#copy-status");
fetch("/update.json",{cache:"no-store"}).then(response=>response.json()).then(release=>{installerHash=release.sha256;document.querySelectorAll("[data-version]").forEach(item=>item.textContent=release.version);document.querySelectorAll("[data-download]").forEach(item=>item.href=release.url);const fileName=new URL(release.url).pathname.split("/").pop();document.querySelectorAll("[data-file]").forEach(item=>item.textContent=fileName);const size=(release.size/1048576).toLocaleString("pt-BR",{maximumFractionDigits:1})+" MB";document.querySelectorAll("[data-size]").forEach(item=>item.textContent=size)}).catch(()=>{status.textContent="Não foi possível carregar os dados da versão."});
button.addEventListener("click",async()=>{if(!installerHash)return;try{await navigator.clipboard.writeText(installerHash);status.textContent="SHA-256 copiado."}catch{status.textContent=installerHash;status.style.overflowWrap="anywhere"}});
if("IntersectionObserver"in window&&!matchMedia("(prefers-reduced-motion: reduce)").matches){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));document.documentElement.classList.add("motion-ready")}
